import * as THREE from 'three';
export function makeProceduralYacht(): THREE.Group {
  return new THREE.Group();
}
export function makeProceduralNV4(): THREE.Group {
  return new THREE.Group();
}
export function makeProceduralGhost(): THREE.Group {
  return new THREE.Group();
}
